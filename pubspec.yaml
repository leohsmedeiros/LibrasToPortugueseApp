name: libras_to_portuguese_app
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  camera: ^0.6.4+5
  path: ^1.7.0
  path_provider: ^1.6.24
  get: ^3.24.0
  opencv: ^1.0.4
  flutter_cache_manager: ^2.1.1
  image: ^2.1.19
  tflite: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/test/
    - assets/training/
    - assets/model_a_b/
    - assets/model/
